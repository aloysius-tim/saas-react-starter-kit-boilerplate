export const SET_TOKEN = (state, token) => {
  state.token = token
}

export const SET_REFRESH_TOKEN = (state, token) => {
  state.refreshToken = token
}

export const SET_ME = (state, me) => {
  state.me = me
}
